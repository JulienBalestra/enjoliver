apiVersion: batch/v2alpha1
kind: CronJob
metadata:
  name: date
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: date
            image: quay.io/julienbalestra/nginx:latest
            command:
            - /bin/date
          restartPolicy: Never
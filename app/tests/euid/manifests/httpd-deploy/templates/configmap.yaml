---
kind: ConfigMap
apiVersion: v1
metadata:
  namespace: default
  name: {{ .Chart.Name }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
data:
  content: |
    Chart.Name: {{ .Chart.Name }}
    version: {{ .Chart.Version }}
---
systemd:
  units:
    - name: probe.service
      enable: true
      contents: |
        [Unit]
        Requires=network-online.target
        After=network-online.target
        [Service]
        Type=oneshot
        ExecStart=/usr/bin/curl -f -XPOST http://172.15.0.1:5050/ok -d "euid-testkvmiso-test_00"
        [Install]
        WantedBy=multi-user.target
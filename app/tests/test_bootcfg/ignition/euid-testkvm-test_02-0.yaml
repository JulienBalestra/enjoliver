---
systemd:
  units:
    - name: probe.service
      enable: true
      contents: |
        [Unit]
        Requires=network-online.target
        After=network-online.target
        [Service]
        Type=simple
        ExecStart=/usr/bin/curl -XPOST http://172.15.0.1:5000/discovery -d "euid-testkvm-test_02-0"
        [Install]
        WantedBy=multi-user.target
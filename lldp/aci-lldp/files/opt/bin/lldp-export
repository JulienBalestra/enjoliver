#!/bin/bash

OUTPUT=/var/run/lldp.xml

for i in {0..90}
do
    lldpctl -f xml | tee ${OUTPUT}
    test $(cat ${OUTPUT} | wc -l) -gt 3 && break
    echo "Fail to run $0 attempt ${i}" >&2
    sleep 1
done
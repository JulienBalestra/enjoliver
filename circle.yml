# Circle CI use containers who doesn't easily allow fs overlay with DGR
# TODO Use rktception

dependencies:
  override:
    - sudo apt-get update -qq
    - sudo make apt

test:
  override:
    - sudo useradd enjoliver
    - MY_USER=enjoliver make prod_setup

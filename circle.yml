# Circle CI use containers who doesn't easily allow fs overlay with DGR
# TODO Use rktception

dependencies:
  override:
    - sudo apt-get update
    - sudo make apt

test:
  override:
    - make submodules
    - make runner
    - make front
    - make assets
    - make check

# Circle CI use containers who doesn't easily allow fs overlay with DGR
# TODO Use rktception

dependencies:
  override:
    - sudo apt-get update -qq
    - sudo make apt

test:
  override:
    - make prod_setup
